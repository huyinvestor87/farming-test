export class ParticleManager{constructor(){this.pool=[]}burst(x,y,color='#fff',text=''){for(let i=0;i<10;i++)this.pool.push({x,y,vx:(Math.random()-.5)*70,vy:-30-Math.random()*60,t:1,color,text:i?'' :text})}update(dt){this.pool.forEach(p=>{p.x+=p.vx*dt;p.y+=p.vy*dt;p.vy+=90*dt;p.t-=dt});this.pool=this.pool.filter(p=>p.t>0)}draw(c){c.save();c.font='bold 16px Nunito';c.textAlign='center';this.pool.forEach(p=>{c.globalAlpha=p.t;c.fillStyle=p.color;if(p.text)c.fillText(p.text,p.x,p.y);else{c.beginPath();c.arc(p.x,p.y,3+p.t*3,0,7);c.fill()}});c.restore()}}
